# Frontend environment variables (Create React App)
# Copy to .env and adjust as needed.
#
# Backend base URL (FastAPI). For local dev, backend runs on port 3001.
REACT_APP_API_BASE_URL=http://localhost:3001
